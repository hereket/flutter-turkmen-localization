name: turkmen_localizations
description: "Turkmen localizations for flutter"
version: 0.0.1
author: Alfred Salahov
homepage:

environment:
  sdk: '>=3.2.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

